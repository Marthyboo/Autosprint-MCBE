# Autosprint

a lightweight autosprint

## Prerequisites
you need to have GCC MinGW-w64 installed, (https://www.msys2.org/).

both g++ and windres should be added to your system's environment variables so they can be accessed

## How to Build
run build.cmd
